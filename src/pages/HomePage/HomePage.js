import React from "react";
import { MainContent, MovieList } from "../../components";

const HomePage = () => {
  return (
    <MainContent>
      <MovieList />
    </MainContent>
  );
};

export default HomePage;
